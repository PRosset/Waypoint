<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %>
</head>

<body>
  <header>
    <% include ../partials/header %>
  </header>
  <div class="formPageWrapper">
    <main>
      <div class="formWrapper">
        <h3>EDIT</h3>
        <form class="campsite-form" action="/campsites/<%= campsite._id %>?_method=PUT" method="post">
          <% include _form %>
          <a href="/campsites/<%= campsite._id %>" class="btn btn-primary">Back</a>
          <button type="submit" class="btn btn-success">Save</button>
        </form>
      </div>
    </main>
  </div>
</body>
<% include ../partials/scripts %>
</html>
